import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.graduationproject.skindiseasedetection',
  appName: 'SKIN DISEASE DETECTION',
  webDir: 'dist'
};

export default config;
